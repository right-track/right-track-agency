'use strict';

/**
 * ### Right Track Agency (Abstract)
 * This module contains classes and functions that are shared across
 * all implementations of a Right Track Agency
 * @module /
 */

module.exports = {

  /**
   * Shared agency configuration functions
   * @see module:config
   */
  config: require('./config.js')

};